{
  "name": "serverless-3tier",
  "version": "0.1.0",
  "private": true,
  "description": "Reference example: URL shortener with a static SPA, a serverless HTTP API, and DynamoDB. Synthesized and tested only -- never deployed. See .claude/specs/serverless-3tier/spec.md (NF1).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest --passWithNoTests",
    "synth": "cdk synth"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^22.20.1",
    "aws-cdk": "2.1135.0",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.12",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1105.0",
    "aws-cdk-lib": "^2.263.0",
    "constructs": "^10.8.1"
  }
}
